<%= form_with(model: short_put, local: true) do |form| %>
  <% if short_put.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(short_put.errors.count, "error") %> prohibited this short_put from being saved:</h2>

      <ul>
        <% short_put.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
